{
  "packages": [
    "packages/*"
  ],
  "npmClient": "yarn",
  "version": "0.0.2",
  "command": {
    "publish": {
      "ignoreChanges": [
        ".gitignore",
        "*.lock",
        "*.md"
      ],
      "message": "chore(release): publish %s"
    }
  }
}
